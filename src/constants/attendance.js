export const ATTENDANCE_PAGE_SIZE = 20

// Backend currently uses a 7-day correction window.
// Keep this configurable because the backend value is still a placeholder.
export const CORRECTION_REQUEST_WINDOW_DAYS = 7